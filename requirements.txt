# ─────────────────────────────────────────────
# TikTok Downloader Bot — зависимости
# Установка: pip install -r requirements.txt
# ─────────────────────────────────────────────

# Telegram Bot Framework (async)
aiogram==3.13.1

# Загрузчик видео/аудио/изображений
yt-dlp>=2024.11.18

# Загрузка переменных окружения из .env
python-dotenv>=1.0.0

# FFmpeg Python-обёртка (для постобработки)
ffmpeg-python>=0.2.0

# Асинхронные HTTP-запросы (внутри aiogram)
aiohttp>=3.9.0

# ─────────────────────────────────────────────
# FFmpeg (системная утилита — не Python-пакет):
#
# Бот ищет ffmpeg в таком порядке:
#   1. FFMPEG_PATH= в .env
#   2. Системный PATH
#   3. ./bin/ffmpeg (кэш прошлого запуска)
#   4. Авто-загрузка статического бинарника
#      → ./bin/ffmpeg (без root, без apt)
#
# Ручная установка при необходимости:
#   Ubuntu/Debian : sudo apt install ffmpeg
#   macOS         : brew install ffmpeg
#   Windows       : https://ffmpeg.org/download.html
# ─────────────────────────────────────────────
